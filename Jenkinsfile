pipeline {
    agent any

    stages {
        stage('Копирую к себе репы с GIT') {
            steps {
               sh 'cp ~/arch/diplom .' 
               // sh 'touch 1.txt && echo "work"'
                
            }
        }  
    }
}
